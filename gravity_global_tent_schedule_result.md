# A minimal global five-phase tent schedule on the 600-cell

Date: 2026-08-12

Frozen audit protocol: `gravity_global_tent_schedule_protocol.md`

Protocol commit: `af50841`

Registered verifier:
`reproducible/verify_gravity_global_tent_schedule.py`

Machine-readable result:
`reproducible/gravity_global_tent_schedule.json`

Targeted run: **26/26 passed**.  The full repository suite was not run.

## Headline

> **DERIVED COMPUTATIONAL:** the conflict graph of the 120 local 600-cell
> tent moves has chromatic number exactly five.  Its 25 maximum independent
> sets of size 24 form exactly ten complete `5 x 24` schedules, and the ten
> schedules constitute one orbit under the full order-14400 `H4` action.

For every one of the `10*5! = 1200` phase orders, the staircase construction
gives a combinatorial product slab with 2400 four-simplices and boundary equal
to two copies of the 600-cell.  A shared Lorentzian metric exists on this
entire slab for every positive pole parameter.

This closes the **combinatorial global-schedule** gap and the corresponding
**kinematic Lorentzian-existence** gap.  It does not yet construct global
dynamics: the regular metric fails every tent-pole vacuum equation, and the
840 internal edge equations of an asymmetric slab have not been solved.

## 1. Provenance and hypotheses

This was not a blind numerical discovery.  Before protocol commit `af50841`,
exploratory calculations had already exposed the numbers 24, 25, 10, the two
phase-parity orbits and one slab f-vector.  They were all declared in the
protocol before the exhaustive global-slab controls were run.

The complete hypotheses are:

1. the spatial slice is the certified regular 600-cell boundary;
2. two vertices conflict when their local tent carriers share a spatial
   tetrahedron;
3. a simultaneous phase is an independent set of that conflict graph;
4. a complete schedule moves each of the 120 vertices exactly once;
5. a vertex replacement inherits the logical vertex's color;
6. the slab uses the standard staircase triangulation of each tetrahedral
   prism;
7. canonicity is measured under the full `H4` action generated by left-right
   quaternion multiplication and quaternion conjugation.

The verifier reconstructs the conflict relation and finds it is exactly the
degree-12 edge graph of the 600-cell.  Thus the scheduling question is a
precise graph-coloring problem, not an analogy.

## 2. Why five phases are forced

The standard 24 Hurwitz units form the binary tetrahedral subgroup `2T` of
the binary icosahedral vertex group `2I`.  No two of those 24 vertices are
adjacent in the 600-cell graph.  Its five cosets therefore exhibit a proper
five-coloring:

```text
120 vertices = 24 + 24 + 24 + 24 + 24.
```

The upper bound is not inferred from that construction.  CP-SAT proves
exactly that the graph independence number is

```text
alpha(G) = 24.
```

Consequently every coloring needs at least

```text
ceil(120 / 24) = 5
```

colors.  Together with the exhibited coset coloring this proves

```text
chi(G) = 5.
```

There is also an external analytic control.  The only vertex scalar product
larger than `1/2` is the edge value `phi/2`.  An independent set is therefore
a spherical code on `S^3` with pairwise scalar products at most `1/2`.  The
four-dimensional kissing-number theorem gives an upper bound of 24
independently of the integer solver; see Oleg Musin,
[The kissing number in four dimensions](https://annals.math.princeton.edu/2008/168-1/p01).

This establishes the minimum concurrency depth.  It does not assign a
physical duration to a phase.

## 3. Complete census: 25 cells and ten covers

The `H4` orbit of the standard `2T` vertex set contains exactly 25 independent
24-sets.  After excluding all 25 in a second exact solver problem, no other
independent set of size 24 exists.

Their finite incidence design is:

```text
25 maximum independent sets;
each vertex lies in 5 sets;
100 unordered pairs are disjoint;
200 unordered pairs intersect in 6 vertices;
each maximum set belongs to 2 complete covers.
```

Exact-cover enumeration gives precisely ten partitions into five disjoint
24-sets.  They are exactly the left- and right-coset partitions associated
with the five conjugate `2T` subgroups.

The numbers 25 and ten are classical 600-cell geometry, not a new polytope
discovery: the 25 inscribed 24-cells and ten compounds of five 24-cells are
known in the literature.  What is new within this repository is their exact
role as the complete census of nonoverlapping global tent schedules.  No
claim of external research novelty is made.

## 4. Canonicity: one unordered orbit, two ordered orbits

The order-14400 `H4` action is transitive on the ten unordered covers.  A
cover stabilizer has order 1440 and acts on its five cells as exactly `A5`:

```text
cover stabilizer             1440
pointwise color kernel         24
induced color group            60 = |A5|.
```

Therefore the geometry supplies one **unordered schedule up to `H4`**.  It
does not select an individual set of five cells; choosing a representative
only fixes a symmetry frame.

Ordering the five phases changes the conclusion.  The 1200 labeled orders
split into two `H4` orbits of 600, distinguished by the parity of the color
permutation.  Reversing five phases has ten inversions and is even, so time
reversal remains inside each orbit rather than exchanging them.

- **DERIVED COMPUTATIONAL:** two phase-order parity classes remain.
- **OPEN:** whether this parity has any physical chirality meaning.
- **NOT CLAIMED:** that geometry selects one of the two classes.

Calling a single ordered schedule canonical would therefore be false.

## 5. The complete product slab

Take an initial spatial tetrahedron.  Its four vertices necessarily have four
different phase colors.  If their order is

```text
v1 < v2 < v3 < v4,
```

moving them in that order divides the tetrahedral prism into four
four-simplices.  Applying this to all 600 tetrahedra gives

```text
600 tetrahedra * 4 = 2400 four-simplices.
```

Every one of the 1200 ordered schedules is explicitly constructed and checked.
The allocation-heavy face census is then reduced exactly to one representative
of each of the two proved `H4` order orbits.  Both yield

```text
slab f-vector = (240, 2280, 6240, 6600, 2400)
Euler characteristic = 0
tetrahedral facets of multiplicity 1 = 1200
tetrahedral facets of multiplicity 2 = 5400.
```

The multiplicity-one facets are exactly

```text
600 initial tetrahedra + 600 final tetrahedra.
```

The edge census is

```text
720 initial spatial edges
720 final spatial edges
720 old-new staircase diagonals
120 tent poles
------------------------------
2280 total edges.
```

Thus there are 840 internal edge variables if both spatial boundaries are
held fixed: 720 diagonals and 120 tent poles.

This is the standard staircase triangulation of the combinatorial product
`S^3 x I`, not merely an f-vector coincidence.

## 6. A global Lorentzian metric exists

Give every initial, final and staircase-diagonal edge squared length `a^2`,
and every pole squared length

```text
-rho a^2,       rho > 0.
```

Every four-simplex then contains exactly one pole and is congruent to the
regular local Lorentzian tent simplex.  In units `a=1`, its exact edge-Gram
determinant and Schur complement are

```text
det(G) = -rho(3 rho + 8)/16,
Schur   = -rho(3 rho + 8)/8.
```

The three-dimensional link block is positive definite.  Hence every simplex
has inertia

```text
(-,+,+,+)
```

for every `rho>0`.  The explicit audit of all 2400 simplices at `rho=1/4`
finds maximum determinant residual `5.6e-17`.

So the five phases do assemble into a coherent piecewise-flat Lorentzian
carrier.  This is a **kinematic existence theorem**, not yet an equation of
motion.

## 7. Why this is not yet the universe's tick

At the regular control `rho=1/4`, each tent pole has deficit

```text
epsilon = 0.196545606092 radians > 0.
```

The earlier exact symmetric Lorentzian no-go applies for every positive
`rho`, not just this control.  Since the full action derivative with respect
to a tent pole is local to its tent carrier, placing 120 regular carriers in
one slab cannot cancel this failure: all 120 regular pole equations are
nonzero.

The asymmetric local witness proves that a single pole equation can vanish,
but it does not supply mutually compatible values for the 720 shared
diagonals, nor does it solve their Regge equations.  The honest remaining
problem is therefore a coupled system on the complete slab, not 120 copies of
the local witness.

The first allocation-heavy implementation attempted a full face-set rebuild
1200 times and terminated before writing a result.  The final verifier uses
the already proved two-orbit `H4` reduction for the expensive face census,
while still building and checking all 1200 schedules individually.  This is
an exact symmetry reduction, not removal of a failed mathematical case.

## 8. Relation to `a1=5`

After the target-free schedule audit, the comparison can be stated precisely:

```text
chosen 600-cell carrier
    -> maximum nonconflicting phase size 24
    -> 120/24 = 5 forced phases.
```

This gives the integer five a genuine dynamical-organizational role: it is
the minimal number of parallel batches needed to advance every vertex once.
But the bridge

```text
a1=5 -> 600-cell
```

is still only structural in the programme.  Therefore:

- **DERIVED within the fixed 600-cell:** five phases are necessary and
  sufficient;
- **STRUCTURAL/PATTERN:** identifying this phase count with the bootstrap
  symbol `a1`;
- **NOT an independent derivation:** the schedule does not prove that
  arithmetic `a1=5` uniquely selects the 600-cell;
- **NOT a speed:** neither a phase nor all five phases has a selected duration.

## 9. Status ledger

| Claim | Status |
|---|---|
| Tent-conflict graph equals 600-cell adjacency | **DERIVED COMPUTATIONAL** |
| Maximum simultaneous phase has 24 moves | **DERIVED COMPUTATIONAL**, with external kissing-number upper bound |
| Exactly five phases are minimal | **DERIVED COMPUTATIONAL** |
| Exactly 25 maximum phases and ten complete covers | **DERIVED COMPUTATIONAL** |
| The ten unordered schedules form one `H4` orbit | **DERIVED COMPUTATIONAL** |
| Ordered schedules form two parity orbits | **DERIVED COMPUTATIONAL** |
| Every order gives a product slab with two exact boundaries | **DERIVED COMPUTATIONAL** |
| The slab admits a Lorentzian metric for every `rho>0` | **DERIVED** under the displayed length assignment |
| The regular metric is a vacuum Regge solution | **DERIVED NEGATIVE** |
| One parity class is physical chirality | **OPEN** |
| The asymmetric 840-edge Regge system has a solution | **OPEN** |
| The five phases have equal physical duration | **OPEN** |
| Five phases equal the bootstrap object `a1` | **STRUCTURAL/PATTERN** |
| A speed limit, Planck time or Planck mass follows | **OPEN** |

## 10. Next decisive calculation

Fix one of the two ordered schedule orbits and enumerate the orbits of the 840
internal edges under its order-24 pointwise phase stabilizer.  This gives the
smallest symmetry-reduced global Regge system allowed by the selected
triangulation.  Then:

1. derive every reduced pole and diagonal equation from the full Lorentzian
   action;
2. test regular data as a negative control;
3. continue the certified asymmetric local witness only if its shared edge
   values are globally compatible;
4. accept dynamics only if all reduced internal equations vanish with every
   four-simplex Lorentzian and both boundary metrics spacelike.

That calculation decides whether the five-phase carrier supports an actual
vacuum evolution or is only a valid causal triangulation.
