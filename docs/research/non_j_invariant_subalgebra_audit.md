# Non-J-invariant order-zero subalgebras

## Question

Does order zero plus noncommutativity force `J A J^-1=A`, or can a
noncommutative algebra commute with a distinct opposite algebra?

## Verdict

The universal forcing statement is **REFUTED**, but the geometrically
generated context scope is **CLOSED**.

## Exact structural counterexample

On `C^6`, split the carrier into three two-dimensional blocks

`B11 direct-sum B12 direct-sum B21`.

Represent `A=C direct-sum M2(C)` by the scalar summand on `B11+B12` and the
matrix summand on `B21`.  Let `J=P K`, where `P` swaps the two scalar copies
and swaps `B12` with `B21`.  The permutation is the product of three
transpositions and is therefore an odd involution.

Then the opposite `M2` acts on `B12`, disjoint from the represented `M2` on
`B21`.  Exact matrix-unit checks give

`[A,J A^* J^-1]=0`, while `J A J^-1 != A`.

Thus a noncommutative, non-`J`-invariant order-zero algebra exists.
**DERIVED algebraic counterexample from the displayed matrices.**

The context geometry does not select its three-block allocation or matching.
Those choices are **STRUCTURAL**.

## Full-gate stopping boundary

The context geometry supplies neither a grading nor a Dirac operator for the
structural three-block allocation.  The mission forbids fitting either one.
The audit therefore stops at the algebraic order-zero result: PD,
orientability, first order, connectedness and fluctuations are not licensed
claims for this model.  **OPEN as a full triple; no derived candidate.**

## Exhaustive geometry-generated screen

The complete 59-subgroup lattice of the derived context `A5` action was
enumerated without assuming two-generation, then independently recovered
from all generator pairs.

- On `C^6`, every noncommutative subgroup algebra was checked against all 76
  involutions in `S6`.
- On oriented `C^12`, every noncommutative subgroup algebra was checked
  against all 1,384 involutions preserving the six orientation pairs; 600 of
  these induce an odd permutation of the six contexts.

No order-zero case exists.  **DERIVED negative in the complete
group-generated/permutation-`J` scope.**

## Evasion boundary and ledger

- **DERIVED POSITIVE:** the explicit structural `C direct-sum M2`
  counterexample refutes the universal forcing theorem.
- **DERIVED NEGATIVE:** no algebra generated by a subgroup of the geometric
  `A5` action evades order zero on either declared carrier.
- **STRUCTURAL:** the successful algebra's block allocation and matching.
- **OPEN:** a geometrically selected non-group-generated subalgebra; a
  derived grading and Dirac satisfying all finite-triple gates.
- **NOT CLAIMED:** a Standard Model algebra or physical gauge sector.

Exact verifier: `reproducible/verify_noncentral_context_J.py`.
