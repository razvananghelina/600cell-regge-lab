# A local unitary Kähler--Dirac tick

Date: 2026-08-11  
Target-blind protocol commit: `ff6b2ce`

## Outcome

The fixed 600-cell cochain geometry admits an exact, nontrivial, strictly
local unitary walk derived from its signed Kähler--Dirac incidence.

This is a **DERIVED LOCAL UNITARY LIFT** of the spatial operator.  It supplies
a dimensionless micro-tick and an exact combinatorial propagation cone.  It
does not yet supply physical time, the numerical value of \(c\), a Planck
scale, a mass operator or a selected fundamental law.

The targeted verifier passes `23/23` in about 1.5 seconds.  No full-suite run
was performed.

## What was corrected

Two old exploratory meanings of “tick” are not valid quantum time evolution:

- repeatedly applying normalized adjacency \(A/12\) is not unitary;
- repeatedly applying \(D\) is not unitary and its norm is dominated by the
  spectral radius.

The accepted continuous-time Whitney evolution \(e^{-itH}\) is unitary, but
its \(t\) and \(c\) are external, and an exponential of a local matrix is not
strictly finite-range at nonzero time.  The construction here resolves the
finite-level **unitarity plus strict locality** problem only.

## Construction

The original Hilbert space contains all oriented simplices:

\[
\mathcal H=C^0\oplus C^1\oplus C^2\oplus C^3,
\qquad
\dim\mathcal H=120+720+1200+600=2640.
\]

Its signed Hasse adjacency is exactly

\[
D=d+d^T.
\]

The walk carrier has one state \(|x,y\rangle\) for every directed incidence
between a simplex and a codimension-one face or coface.  Its dimension is
14880.  If \(q_x\) is the number of incidences at \(x\), define

\[
|s_x\rangle=\frac1{\sqrt{q_x}}
\sum_{y\sim x}\eta_{xy}|x,y\rangle,
\]

where the two arc signs multiply to the oriented incidence sign.  There are
no free coin angles.  The local reflection, reversal and tick are

\[
C=2\sum_x|s_x\rangle\langle s_x|-I,
\qquad
S|x,y\rangle=|y,x\rangle,
\qquad
U=SC.
\]

For each simplex, if \(\eta\) is its vector of signs, the integer numerator

\[
B_x=2\eta\eta^T-q_x I
\]

satisfies exactly

\[
B_x^2=q_x^2 I.
\]

Therefore \(C^2=S^2=I\) and \(U\) is unitary.  The assembled numerical audit
has residual

\[
\max|U^*U-I|=2.22\times10^{-16},
\]

but unitarity does not rely on that tolerance; it follows from the preceding
integer identity.

## Exact cone and calibration

Every nonzero matrix element of one tick changes the tail simplex across
exactly one Hasse incidence.  Consequently

\[
\operatorname{supp}(U^n\psi)
\subseteq
\{x:\operatorname{dist}_{\rm Hasse}(x,\operatorname{supp}\psi)\le n\}.
\]

One micro-tick also exchanges even and odd form degree exactly.

For a localized 600-cell incidence state, the support sizes through four
ticks are

```text
1, 12, 84, 220, 683
```

while the norm remains one.

On the known-answer control, an oriented cycle with 16 vertices, the same
degree-two reflections give deterministic left- and right-moving states:

```text
left  after 0,2,4,6,8 micro-ticks: 0,15,14,13,12
right after 0,2,4,6,8 micro-ticks: 0, 1, 2, 3, 4
```

Thus the speed is exactly one Hasse edge per micro-tick, equivalently one
original cycle edge per two incidence micro-ticks.  No peak or fitted slope
is read.

## Relation to the theory's own operator

Let

\[
Q=\operatorname{diag}(q_x),
\qquad
A|x\rangle=|s_x\rangle.
\]

The walk discriminant is exactly

\[
A^*SA=Q^{-1/2}D Q^{-1/2}=:T.
\]

For \(Tf=\lambda f\), the invariant plane generated by \(Af\) and \(SAf\)
carries the matrix equation

\[
\mu^2-2\lambda\mu+1=0,
\]

so its walk phases are

\[
\mu=e^{\pm i\arccos\lambda}.
\]

This is not an arbitrary graph walk placed next to the theory: its phases
are the exact spectral transform of the normalized signed Kähler--Dirac
operator.  The exact Betti numbers are

\[
(b_0,b_1,b_2,b_3)=(1,0,0,1),
\]

so its two harmonic modes remain the two zero modes of \(T\).

## Hostile carrier audit

The local unitary dilation is much larger than the original cochain carrier.
An explicit signed Hasse four-cycle has product \(-1\), excluding endpoint
eigenvalues \(\lambda=\pm1\).  Therefore the canonical invariant sector

\[
\operatorname{span}(A\mathcal H,SA\mathcal H)
\]

has dimension exactly

\[
2\times2640=5280.
\]

The orthogonal walk-only complement has dimension

\[
14880-5280=9600.
\]

That is not a small correction: most arc states are additional coin/cycle
modes not supplied by the original Kähler--Dirac spectrum.  Initial states
encoded as \(Af\) stay in the 5280-dimensional invariant sector, but the
theory has not yet explained why only that sector is physical.  This is an
**OPEN carrier-selection problem**, not something to hide behind unitarity.

## What this says physically

- **DERIVED:** a coefficient-free unitary lift exists.
- **DERIVED:** information cannot cross more than one Hasse incidence per
  micro-tick.
- **DERIVED:** the low-quasienergy content is spectrally tied to normalized
  Kähler--Dirac incidence.
- **DERIVED:** the cycle control is exactly ballistic.
- **STRUCTURAL:** interpreting this particular quantization functor as the
  law of nature.
- **OPEN:** why the 5280-dimensional own-operator sector is physical and the
  9600 additional modes are absent.
- **OPEN:** compatibility under barycentric refinement and emergence of an
  isotropic three-dimensional low-energy cone.
- **OPEN:** Lorentzian reconstruction and an internally selected mass term.
- **NOT DERIVED:** metres, seconds, kilograms, \(c\), \(G\), \(\hbar\),
  Planck time or Planck mass.

The construction gives only the dimensionless statement

\[
c_{\rm lattice}=1\ \text{Hasse edge per micro-tick}.
\]

To turn this into SI units one must still establish a physical edge length
\(\ell_*\) and tick duration \(\tau_*\), after which

\[
c=\frac{\ell_*}{\tau_*}.
\]

Calling \(\ell_*\) the Planck length or \(\tau_*\) the Planck time would at
present be an assumption.  The Planck quantities require a dimensionful
gravitational normalization:

\[
t_P=\sqrt{\frac{\hbar G}{c^5}},
\qquad
m_P=\sqrt{\frac{\hbar c}{G}}.
\]

## Next kill gate

Apply the same target-blind construction to the known-answer dyadic circle
refinement and then to one barycentric tetrahedron.  Test whether the
low-quasienergy sector compresses or converges under the already-derived
Whitney inclusions after accounting for the geometric subdivision of one
coarse incidence into fine incidences.

- A stable, scale-independent cone would advance the spacetime route.
- Failure under the circle control kills the proposed refinement reading
  immediately.
- Passing the circle but failing the tetrahedral refinement leaves a valid
  lattice walk, not a continuum spacetime.

## Reproduction

```bash
/home/razvan/science/.venv/bin/python \
  reproducible/verify_kahler_dirac_local_tick.py
```

Expected result: `23/23`.

