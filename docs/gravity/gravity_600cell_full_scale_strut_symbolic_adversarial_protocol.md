# Adversarial protocol: generic two-cell proof of the scale--strut response

Date: 2026-08-20

This target-disclosed protocol is committed after the finite global controls
and precision audit, but before any new symbolic two-cell matrix is built.
It is the mechanically different replication required by Rule 4.

## Frozen inputs

| input | SHA-256 |
|---|---|
| exploratory disclosure | `e3dba59118e35cc2370beec4b081cc18fdfd1753fda74a6ca2b0a013d86bd473` |
| full-carrier prior-art gate | `3fc6c3e75ad92c3c20bb420d97e26e73fdd62b69a9aac44162fa95c74c29219a` |
| finite first-result note | `5753375ca2a6c4f5152f134474176501b580a1c55b7a871b3a39fa6321d82f61` |
| finite primary source | `e68105df4058f7d2ed39a6913f29e88cd9fe88e123ff52260acf698a2bd7da49` |
| finite primary artifact | `6289b23596da28d448d1f624ecf9d9e4873ab2aa0478906dd9e90f6e13f6838d` |
| resolved precision source | `7d8f64470251cb79be267be9c4937c09caf2827f0d4da1e6479d1555db1cd80c` |
| resolved precision artifact | `2a2a79271a92fc2ddde343a9d0651402df6eeb4a90efa2697e26f54cafcdf60f` |
| accepted two-frustum result | `b5bb18c75ea1359d33b9985ad5816c21f437960c06f8c4eae793a3505509add3` |

The finite artifact must retain zero mismatches at all three unseen rational
representatives and exact rank 240.  The precision artifact must retain
`9/9` and `FULL_SCALE_STRUT_PRECISION_RESOLVED`.

## Generic hypotheses

Work over the rational function field `Q(lambda,tau)` with

```text
lambda != 1,
tau != 0,
(lambda-1)^2 - 3 tau^2 != 0.
```

Use `eta=diag(1,1,1,-1)` and the canonical regular tetrahedron

```text
p0=( 1, 1, 1,0), p1=( 1,-1,-1,0),
p2=(-1, 1,-1,0), p3=(-1,-1, 1,0),
q_i=lambda p_i+tau n.
```

Two cells share local vertices `(0,1,2)`.  The target apex is developed by
the exact Lorentzian affine reflection across the lateral hyperplane through
`p0,p1,p2,q0`.  No global 600-cell builder, frozen local block, fitted
coefficient or action source may be imported.

If the symbolic construction reveals any additional irreducible denominator
or rank-exception polynomial, the outcome is `HYPOTHESIS_GAP`; the theorem
may not silently acquire a post-result assumption.

## Mechanically independent local solve

For each upper vertex `j`, solve its four displacement components from:

```text
the three lower-i to upper-j cross-diagonal differentials, i!=j,
the one signed strut squared-length differential at j.
```

This gives a `16 x 8` physical response depending linearly on the disclosed
endpoint coefficients `(A,B,C,D)`.  It does not form the primary universal
`6 x 8` flex block and does not eliminate 51,320 rows.

Substitute that response into the six upper-edge differentials.  Over
`Q(lambda,tau)`, their ideal in `(A,B,C,D)` must be exactly

```text
< A+B-8, C+D-1 >.
```

The ideal equality must be checked by a monic Gröbner basis, not by a rank at
one numerical point.  This is the positive control that one cell remains
two-parameter underdetermined.

## Shared-face gluing with the connection retained

Construct the exact affine transition from the target cell frame to the
source development and require:

```text
linear^T eta linear = eta,
|det(linear)|=1,
all three shared lower and upper vertices map exactly.
```

Independently reconstruct the ten-dimensional Poincare evaluation on the
three shared lower vertices.  Its kernel must be one-dimensional.  Evaluate
that generator on the shared upper vertices to obtain one nonzero connection
column and eliminate only that column with an exact left annihilator.

After imposing `A=8-B` and `C=1-D`, form every annihilated shared-displacement
residual for the ten global endpoint-data columns.  Over the fraction field,
their monic Gröbner basis in `(B,D)` must equal the basis generated by

```text
(lambda-1)^2 B - 2 (lambda-1)^2 - 2 tau^2,
(lambda-1) D - lambda.
```

Equivalently, the unique generic solution must be

```text
A = 6 - 2 tau^2/(lambda-1)^2,
B = 2 + 2 tau^2/(lambda-1)^2,
C = -1/(lambda-1),
D =  lambda/(lambda-1).
```

Record every nonzero residual numerator, the two reduced Gröbner bases and
all denominator factors.

## Adversarial controls

1. **Omit the connection column.** Direct equality of the two local frame
   responses must not yield the accepted ideal.  This catches accidental
   fixed-frame gluing.
2. **Corrupt `D -> D+1`.** Substitution into the correct annihilated
   residuals must leave a nonzero numerator.
3. **Time reversal.** Repeating the construction with `tau -> -tau` must
   preserve the reduced coefficient ideal.
4. **Odd local relabelling.** Swap canonical labels `0` and `1` in both cells
   and consistently permute endpoint data; the same four scalar coefficients
   must result.
5. **Finite controls only after the symbolic result.** Substitution at
   `(4,7),(5,13),(7,17)` must reproduce the already-frozen coefficient
   quadruples.  These substitutions are regression checks, not evidence for
   the generic theorem.

## Outcome hierarchy

1. `FULL_SCALE_STRUT_SYMBOLIC_CONTROL_FAILED`: provenance, regular geometry,
   local ideal, Poincare stabilizer, affine Lorentz transition, corruption or
   convention control fails.
2. `FULL_SCALE_STRUT_SYMBOLIC_HYPOTHESIS_GAP`: an unlisted denominator or
   generic rank-exception factor is required.
3. `FULL_SCALE_STRUT_SYMBOLIC_DISAGREEMENT`: controls pass but the correct
   gluing ideal differs from the disclosed one.
4. `FULL_SCALE_STRUT_SYMBOLIC_CORROBORATED`: all exact ideal and adversarial
   checks pass under exactly the frozen hypotheses.
5. `FULL_SCALE_STRUT_SYMBOLIC_OPEN`: any remaining classified case.

Only outcome 4, combined with the finite global and precision artifacts,
accepts the generic kinematic carrier.  It still supplies no action,
canonical stationarity, gauge classification, propagation, clock, tick,
`c`, `G`, Planck scale, graviton or particle mass.

Only the new verifier and static registry guards may be run.

