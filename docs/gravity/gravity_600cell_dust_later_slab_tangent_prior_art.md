# Prior-art gate: third-slab tangent and shifted Jacobi background

Date: 2026-08-18

## Exact object and complete hypotheses

The repository already contains three consecutive, non-static, homogeneous
fixed-total-mass dust-Regge slabs on the fixed regular 600-cell.  It also
contains complete action-derived tangent balls for the first two slabs,

```text
T_1 : Phase(slice 0) -> Phase(slice 1),
T_2 : Phase(slice 1) -> Phase(slice 2),
```

and a centered Jacobi equation formed from them.  Therefore the next missing
object is not another first two-slab calculation.  It is

```text
T_3 : Phase(slice 2) -> Phase(slice 3)
```

evaluated at the separately accepted third homogeneous slab, followed by the
target-free product

```text
C_32 = T_3 T_2.
```

Every phase carrier has `720+720=1440` real position/momentum dimensions.
The positions are the literal logarithmic signed-squared spatial edge
variables, and the momenta are generated by the same complete Regge-plus-dust
action.  The calculation is conditional on all of the following:

1. the fixed regular 600-cell and the already derived two staircase schedules;
2. the same `30` boundary-edge orbits, `35` internal-edge orbits and seven
   binary-tetrahedral minimal sectors used upstream;
3. the accepted absolute second state `(a_2,r_2)` and third state `(a_3,r_3)`;
4. one conserved dust mass, never recomputed from a later scale;
5. the literal boundary identification and the complete thirty-component
   canonical seam between slabs 2 and 3;
6. all four frozen derivative variants and Flint-ball propagation;
7. the same Lorentzian branch, determinant and symplectic controls as for
   `T_1,T_2`;
8. no old negative-mode carrier, desired branch count, spatial spectrum,
   continuum harmonic target, limiting speed or Lyapunov target.

The third state is genuinely different:

```text
a_2 = -9.3481870589e-6,   r_2 = -1.4237027552e-5,
a_3 = -1.8696399104e-5,   r_3 = -3.2033368285e-5.
```

Thus `T_3` is a later-background derivative, not a duplicate of `T_2`.

## Primary literature

- [Marsden--West](https://doi.org/10.1017/S096249290100006X) derive
  discrete Legendre maps, their symplecticity and the discrete Jacobi
  equation from a variational principle.  This makes a product of successive
  background-dependent tangents the standard object; it does not determine
  the present matrices.
- [Dittrich--Hoehn](https://arxiv.org/abs/1108.1974) derive composable
  action-generated canonical evolution for simplicial gravity.
- [Dittrich--Hoehn](https://arxiv.org/abs/0912.1817) show that linearized
  Regge dynamics and pseudo-constraints depend on the background about which
  the action is expanded.
- [Bahr--Dittrich](https://arxiv.org/abs/0905.1670) show why curved Regge
  backgrounds generically do not retain exact continuum gauge symmetry.
- [Hoehn](https://arxiv.org/abs/1411.5672) identifies lattice gravitons only
  after a curvature and gauge analysis of the canonical linearized theory.
- [De Felice--Fabri](https://arxiv.org/abs/gr-qc/0009093) and their
  [generalized 600-cell evolution](https://arxiv.org/abs/gr-qc/0106077)
  evolve dust-filled 600-cell cosmologies over many steps.  They do not print
  the present complete anisotropic tangent balls.

Hence composable non-autonomous Regge tangents are **KNOWN** formalism.
No located primary source contains this full fixed-action `T_3` or its
calibrated seven-sector ball decomposition.  External novelty is **OPEN**;
absence from a search is not proof.

## What is already known and what is not

- **DERIVED UPSTREAM:** the first three homogeneous slabs solve their complete
  internal equations and sew canonically with one conserved mass.
- **DERIVED UPSTREAM:** `T_1`, `T_2` and `T_2T_1` are regular and symplectic
  on the complete declared carrier.
- **DERIVED UPSTREAM:** the first centered Jacobi operator has an
  action-selected conformal/shape split and `30` resolved negative-stiffness
  shape directions.
- **OPEN:** existence, regularity and symplecticity of the complete `T_3`.
- **OPEN:** whether `T_3T_2` agrees between the two independently derived
  schedules.
- **OPEN:** whether a Jacobi operator centered one slice later retains the
  conformal/shape split or the old negative carrier.
- **OPEN:** any long-time exponent, physical polarization, continuum
  dispersion, causal speed or refinement law.

## Framing attack

One more symplectic tangent is not, by itself, new gravitational physics.
Likewise, eigenvalues of `T_3T_2` are not a Lyapunov spectrum: the background
is nonstationary and the product contains only two updates.  A blind branch
count may be recorded as a numerical diagnostic, but it cannot decide the
physics gate.

The load-bearing purpose of `T_3` is instead to make a shifted three-slice
Jacobi equation possible:

```text
K_-^(2) delta q_1 + K_0^(2) delta q_2 + K_+^(2) delta q_3 = 0.
```

That shifted operator must be committed before comparing it with the
previously disclosed negative carrier.  Only a later comparison can test
whether the same action-selected subsystem persists along the evolving
background.

## Admissible next protocol

The first protocol will construct only `T_3` and `T_3T_2`, with the complete
seam, branch, determinant, canonicality, archive and schedule controls.  It
will not load the earlier shape/negative-mode artifacts.

If that gate passes, a second blind protocol will reconstruct the shifted
Jacobi coefficients from `T_2,T_3`.  Only after that artifact is committed
may a target-disclosed verifier compare its conformal/shape and negative
subspaces with the earlier ones and form a finite non-autonomous negative-mode
cocycle.

