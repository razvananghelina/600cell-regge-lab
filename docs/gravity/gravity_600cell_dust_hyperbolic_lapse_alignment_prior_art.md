# Prior-art gate: do the full tangent's extreme modes come from the weak lapse sector?

Date: 2026-08-17

## Exact object and disclosed provenance

The full-boundary blind census was committed at `360253d` before this
question was formulated.  It found, in each minimal irreducible sector of
dimension `d`, `5d` expanding and `5d` contracting/open-extreme eigenmodes.
After restoring representation multiplicity, there are 119 strong
hyperbolic reciprocal pairs plus one numerically open homogeneous pair.

The independent Schur audit had already selected five pole-edge orbit types
per sector and found a `5d` weak but regular block, totaling 120 directions.
The equality

```text
119 strong pairs + 1 open pair = 120 weak directions
```

was noticed **after** both calculations.  The present mission is therefore
confirmatory and target-aware.  It cannot turn that equality of counts into
blind evidence retroactively.

For each parity and minimal sector reconstruct, under all four frozen
derivative estimates:

```text
J Y = R,
Y : boundary phase variations -> (internal,new) variations.
```

Define before inspecting eigenvectors:

1. the **canonical weak lift** `C`, obtained by selecting the five
   geometry-fixed pole columns of `J` and applying the exact Schur lift
   `(-A^-1 B, I)`;
2. the independently constructed **geometric vertex-lapse space** `G`, in
   which moving one new vertex changes its pole edge and its incident
   diagonals with the already derived fixed coefficient;
3. the `5d` most expanding and `5d` most contracting invariant subspaces of
   the boundary tangent, selected only by ordered modulus and the fixed count
   of five pole orbits.

Transport each spectral subspace through `Y` and compare it separately with
`C` and `G` by principal angles in the frozen logarithmic edge-coordinate
norm.

## Primary prior art

- Bahr and Dittrich, [Breaking and restoring of diffeomorphism symmetry in
  discrete gravity](https://arxiv.org/abs/0905.1670), show that curvature can
  lift exact Regge gauge symmetry and replace constraints by
  background-dependent pseudo-constraints.
- Dittrich and Hoehn, [From covariant to canonical formulations of discrete
  gravity](https://arxiv.org/abs/0912.1817), derive pre/post constraints and
  pseudo-constraints from the covariant action and its Hessian.
- Hoehn, [Canonical linearized Regge Calculus: counting lattice gravitons
  with Pachner moves](https://arxiv.org/abs/1411.5672), separates vertex
  displacement generators from gauge-invariant lattice gravitons on flat
  backgrounds.
- Dittrich, Freidel and Speziale, [Linearized dynamics from the 4-simplex
  Regge action](https://arxiv.org/abs/0707.4513), relate Regge-Hessian weak or
  null directions to remnant diffeomorphisms.
- Barrett et al., [A Parallelizable Implicit Evolution Scheme for Regge
  Calculus](https://arxiv.org/abs/gr-qc/9411008), discuss lapse/shift freedom,
  Bianchi identities and implicit 600-cell evolution.

Thus weak Hessian directions, broken gauge modes and canonical Regge
evolution are **KNOWN**.  The sources do not license identifying a large
transfer eigenvalue with gauge merely because their counts agree.  No
located primary source gives the present subspace comparison.  External
novelty remains **OPEN**.

## KNOWN / CONTROL / OPEN

- **KNOWN:** exact gauge modes on flat Regge backgrounds and lifted
  pseudo-constraints on curved backgrounds.
- **CONTROL:** the full `1560 x 1560` pre-Legendre matrix is regular, including
  a 120-dimensional weak Schur sector.
- **CONTROL:** the `1440 x 1440` boundary tangent is symplectic and has a
  stable extreme spectral count in both schedules.
- **CONTROL:** `C` and `G` are independently defined before this comparison;
  they were previously only `NUMERICALLY_OPEN` relative to one another, with
  projector distance about `1.2e-5`.
- **OPEN:** whether either expanding or contracting internal response equals
  `C` or `G` sector by sector.
- **OPEN:** whether the extreme modes are physical, pseudo-constraint, or
  mixed.

## Framing attack

Principal angles require a norm.  The logarithmic edge-coordinate norm is
already frozen and order-24 invariant, but it is not a derived physical norm
and is not invariant under arbitrary canonical coordinate changes.
Consequently:

- exact/calibrated subspace equality would be a coordinate-level algebraic
  result;
- separation would refute the proposed exact identification in these frozen
  variables;
- a merely small angle is **STRUCTURAL/PATTERN**, not a proof of gauge;
- no outcome by itself identifies a graviton or a physical instability.

It would be circular to combine `C` and `G` into a manufactured `10d` space
only because the stable-plus-unstable space has dimension `10d`.  That test is
forbidden.  Expanding and contracting branches are compared separately with
each independently defined `5d` candidate.

No spatial Laplacian, continuum harmonic, desired degeneracy, speed or
experimental target may enter.  Only after this gate can a physical
curvature observable be chosen.
