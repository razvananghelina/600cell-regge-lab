# Prior-art gate: target-independent stationary momentum envelope

Date: 2026-08-16

Accepted first tick: `46a7361`.

Direct and homotopy solver boundaries: `6346ad0`, `7a39520`.

Status: **completed before evaluating the stationary curve or reading the
second-tick target in the new verifier**.

## 1. Exact object

Fix the lower geometry of the desired second slab at the committed `L1`, the
complete local Lorentzian Regge+dust action and the conserved total dust mass.
Do not load any desired incoming momentum.

Use absolute logarithms

```text
b = log(L_upper/L0),
r = log(rho/rho0).
```

For fixed lower log `a1`, the exact geometry is

```text
q_old       = exp(2*a1)*L0^2,
q_new       = exp(2*b)*L0^2,
pole        = exp(r)*rho0,
diagonal    = exp(a1+b)*L0^2-exp(r)*rho0.
```

Define target-independent scalar functions

```text
G(b,r) = mean of the five complete pole equations,
P(b,r) = mean of the thirty canonical pre-momenta.
```

The internally stationary homothetic curve is `G=0`.  Along a regular branch
with `G_b != 0`, its momentum derivative has the sign of

```text
D(b,r) = G_b*P_r-G_r*P_b.
```

Zeros of `D` on `G=0` are folds/extrema of the canonical pre-momentum as a
function of the lapse log.  Their definition contains no desired target.

## 2. KNOWN

Pre/post canonical momenta generated by a discrete action and loss of local
invertibility through Hessian/rank degeneracy are standard in canonical
simplicial gravity:

- Dittrich and Hoehn, *Canonical simplicial gravity*,
  <https://arxiv.org/abs/1108.1974>;
- Dittrich and Hoehn, *From covariant to canonical formulations of discrete
  gravity*, <https://arxiv.org/abs/0912.1817>.

On curved Regge backgrounds, exact gauge constraints can become
background-dependent pseudo-constraints:

- Bahr and Dittrich, <https://arxiv.org/abs/0905.1670>.

Regge FLRW models can terminate when struts approach null and can depend
strongly on finite resolution and on local versus global variation:

- Liu and Williams, <https://arxiv.org/abs/1501.07614>;
- De Felice and Fabri, <https://arxiv.org/abs/gr-qc/0106077>.

Thus folds, weak Legendre directions and lapse-collapse boundaries are known
structural dangers.  Computing the present fixed-carrier envelope is not a new
general mechanism.

## 3. CONTROL

The envelope verifier must not load the second-tick target or the failed
solver trajectories.  It may load only the accepted first-tick geometry
`(a1,r1)` to define the fixed lower boundary and a target-independent starting
lapse.

It must:

1. reconstruct the complete action and both staircase schedules;
2. solve `G=0` at every frozen lapse node with calibrated scalar derivatives;
3. compute `(G,P)` derivatives with the four already validated precision
   scales;
4. retain the complete Lorentzian branch and all 35 internal equations;
5. compare even/odd values by substitution at the same curve points;
6. write the full curve, fold brackets and momentum multiset before any target
   comparison.

## 4. OPEN scope

The first calculation will cover a frozen decreasing-lapse log domain and
separate near-null sentinels.  It can establish resolved folds and extrema on
that computational domain.  It cannot by finite sampling alone prove a global
analytic bound on the entire unbounded interval `r -> -infinity`.

Accordingly:

- a resolved fold is **DERIVED COMPUTATIONAL**;
- absence of additional folds on the sampled domain is **DERIVED
  COMPUTATIONAL ON THE FROZEN GRID**, not a theorem between nodes;
- apparent convergence at near-null sentinels is **PATTERN**;
- a global analytic envelope remains **OPEN** without monotonicity or interval
  bounds.

Only after the target-independent artifact is committed may a separate
comparison ask whether the inherited target lies below or above its extrema.

## 5. Framing attack

If the target exceeds merely a sampled maximum, that is not a no-go theorem.
A kill verdict requires either a certified global envelope or an independent
selection principle that forbids crossing a resolved fold.  The new mission is
designed first to measure the obstruction honestly, not to manufacture a
closure claim from two failed Newton solvers.
