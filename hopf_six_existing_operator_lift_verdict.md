# No existing theory operator lifts oddly to the 936-state carrier

Date: 2026-08-11

Protocol commit: `e54695a`.

Registered verifier:
`reproducible/verify_hopf_six_existing_operator_lift.py`.
Targeted result: `10/10`.

No Hessian target, matter character, mass or coupling comparison was used.

## Headline

The repository now has:

- a geometrically selected real algebra
  `M6(R)^2+M12(R)^2`;
- canonical incidence labels for all four simple blocks;
- a robust 936-state KO6 carrier with orientability and Poincare duality;
- a canonical equivariant rook operator that passes first order but fails
  connectedness.

The complete preregistered inventory finds no other already-defined operator
that can repair the failure:

```text
existing operator families audited                 7,
faithful canonical lifts with nonzero odd part      0,
families with no defined lift                        5,
algebra-generated families with zero odd part        2.
```

This is a **DERIVED REPOSITORY-STATE NEGATIVE**.  Continuing now requires a
new noncentral incidence tensor, not a reinterpretation of an operator that
was already present.

## Frozen inventory and outcomes

| Existing family | Certified carrier | Result on `H_off` |
|---|---:|---|
| `A`, `A_F`, `Box_F` | 120 | no faithful `B_R` representation or lift |
| Kähler--Dirac `d+d*` and moments | 2640 | no six-fibration crossed-product lift |
| `Phi(X)`, `D_aux` | 6, 12 | natural `M6` quotient; crossed-product kernel 324 |
| full label Hessian and affine odd double | 5, 10 | `W5` label module; no faithful `B_R` lift |
| `C6` pair rook and standard double | 36, 72 | represents `C^6`, not `B_R` |
| natural/minimum/regular crossed-product actions | 6, 36, 360 | faithful cases are algebra-generated and grading-even |
| `u_edge`, `u_chord`, `v_ref` and functional calculus | 10, 60 | faithful Morita realization exists, but class convolutions are grading-even |

The source audit of the seven authoritative operator-defining scripts finds
no explicit map into the later 936-state cell carrier.  This absence claim is
deliberately scoped to the frozen files; it is not a theorem that no such map
can ever be constructed.

## Algebraic evenness theorem

On

```text
H_off=direct_sum_(i!=j) H_ij,
```

the left algebra acts on the first central index and the opposite algebra on
the second.  Both preserve every ordered cell `(i,j)`.  The grading is scalar
on each cell, hence

```text
[gamma,pi(B_R)]=0,
[gamma,pi(B_R)^o]=0.
```

It follows by algebraic closure that every polynomial or convolution
generated by the left and right crossed-product actions has zero odd
projection.

The verifier exhausts all simple-block matrix units at cell-support level for
all eight spectral readings:

```text
17,280 generator-cell actions,
17,280 grading-preserving,
0 grading-changing.
```

Thus the internal spectra of `u_edge`, `u_chord` and `v_ref` can label nodes,
but those same operators cannot serve as the odd Dirac coupling between
cells.

## Two convention traps closed

### The two 36-dimensional spaces

The old pair-groupoid carrier `C^6 tensor C^6` and the minimum faithful
crossed-product module both have dimension 36.  They represent different
algebras and have different bimodule structures.  No committed intertwiner
identifies them.  Dimension equality supplies no lift.

### The 12-state auxiliary operator

`D_aux` has dimension 12, which equals the dimension of either large simple
module.  Its certified algebra action is nevertheless two copies of the
natural six-label quotient.  The full crossed-product kernel remains 324.
Reassigning those twelve coordinates to an `M12` block would be a new
representation chosen after the fact.

## Hostile framing audit

1. This is not a universal operator no-go.  It covers exactly seven frozen
   committed families.
2. Absence of a source-level lift is weaker than a mathematical impossibility
   theorem; that is why the verdict is explicitly repository-state scoped.
3. Algebra-generated evenness is a mathematical theorem on this carrier and
   does not depend on source naming.
4. A conditional expectation, Fourier transform, universal differential or
   stabilizer delta projection introduced now would be a new construction.
   It may be worth testing, but cannot be described as already derived.
5. The positive carrier result remains valid.  What is absent is the physical
   operator, not orientability or Poincare duality.

## Status ledger

- **DERIVED:** every left/right `B_R` action preserves the twelve central
  cells and is grading-even.
- **DERIVED:** the algebra-generated odd projection is identically zero.
- **DERIVED REPOSITORY-STATE NEGATIVE:** the five non-algebraic existing
  families have no committed faithful map to `H_off`.
- **DERIVED:** the `36=36` and `12=M12 dimension` coincidences do not define
  algebra intertwiners.
- **DERIVED SCOPED RESULT:** accepted existing lift count is `0/7`.
- **OPEN:** a new noncentral incidence tensor with a faithful odd lift.
- **NO TARGET COMPARISON:** no physical target was examined.

## Programme boundary

The present six-fibration route is no longer missing an algebra or a
Poincare-dual carrier.  It is missing dynamics at the exact point where
dynamics should enter: a geometrically normalized operator that mixes the
complementary simple-module sectors strongly enough to make the algebra
commutant scalar.

Until such a tensor is independently constructed, the route is a coherent
finite noncommutative arena rather than a spectral theory of matter.
