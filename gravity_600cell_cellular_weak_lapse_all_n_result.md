# All-index cellular weak-lapse coefficient theorem

Date: 2026-08-17

Prior-art gate commit: `bd1e7df`  
Protocol commit: `a07b3ff`  
Leading-rank protocol correction: `c0303a4`  
Registered verifier commit: `51904c0`  
Serialization-only correction: `b477574`

Artifact:

```text
reproducible/gravity_600cell_cellular_weak_lapse_all_n.json
SHA-256 fd2fe57c3ea2e46d87efb5f0a392fc454c747376aa39848b8cdcfec0922041bb
```

## Verdict

**DERIVED:** `CELLULAR_WEAK_LAPSE_ALL_N_PROVED` (`8/8` targeted checks,
`16.87 s`).

The homogeneous conserved-dust cellular action has the preregistered formal
weak-lapse coefficient solution for every fixed integer `n>=1`.  The previous
four-tick law is therefore not a four-term coincidence.

## The theorem

Let

```text
epsilon = 2*pi-5*acos(1/3),
q       = 5*sqrt(2)-3*epsilon,
x       = e^2,
t_n     = n(n+1).
```

For the contracting orientation of the canonical map generated by the
cellular action, with a static initial slab and

```text
log(L_n/L0)          = A_n x+B_n x^2+O(x^3),
log(rho_n/(e^2 L0^2)) = R_n x+O(x^2),
```

the coefficients are

```text
A_n = -6*epsilon*t_n/q,

R_n = -10*epsilon*(7*sqrt(2)*epsilon+60)*n^2/q^3,

B_n = -epsilon^2*t_n/q^4 *
      [(108*epsilon^2-395*sqrt(2)*epsilon+300)*t_n
       -54*epsilon^2+145*sqrt(2)*epsilon-600],

p_out,n/k = 2*n+1.
```

Equivalently,

```text
A_n-A_(n-1) = n A_1,
R_n-R_(n-1) = (2n-1)R_1.
```

The time-reversed orientation changes the physical direction of the same
local solution; it was not separately enumerated.

## Exact induction certificate

The verifier reconstructs the action jet and keeps `n` symbolic.  After
substituting the candidate at `n-2` and `n-1`:

- the leading seam slope is
  `-30(3*epsilon-5*sqrt(2))`, hence is nonzero;
- its unique root is the displayed `A_n`;
- the leading lapse equation vanishes on the same root;
- the joint local leading rank is one;
- the next equations are affine in `(B_n,R_n)`;
- their exact determinant is `16200*epsilon^2`;
- both Cramer residuals against the displayed `B_n,R_n` are exactly zero;
- the outgoing momentum ratio reduces identically to `2*n+1`.

All reductions are in `Q(sqrt(2))(epsilon,n)`.  The largest unreduced generic
coefficient equations contain 625 and 685 SymPy operations, so the result is
not an interpolation over integer samples.

The domain is nondegenerate:

```text
epsilon = 0.12838822047571306625... > 0,
q       = 6.68590315043833604526... > 0.
```

The signs follow from the elementary angle bounds recorded in the protocol;
therefore `A_n<0` for every positive `n` on this orientation.

## Independent controls

- All twelve frozen `A_n,B_n,R_n` coefficients at `n=1..4` are recovered
  exactly.
- The full, unexpanded action was evaluated at the new indices `n=5,7,11`,
  which were not used in the interpolant.
- Under `e: 1/200 -> 1/400 -> 1/800`, lapse residual orders approach `7` and
  seam residual orders approach `5` at all three indices.  The worst observed
  pair is still `(6.99941498,6.99985376)` for lapse and
  `(4.99932351,4.99983090)` for the seam at `n=11`.

The first mathematical run passed every theorem check and then failed only
because a SymPy boolean was not JSON serializable.  Commit `b477574` converts
output types to native Python booleans/integers; no equation or criterion was
changed.

## Scientific meaning

**DERIVED:** in the homogeneous weak-lapse sector, the cellular 600-cell
action produces constant discrete acceleration for every formal step index,
not just the four numerically solved steps.

**STRUCTURAL:** this is an exact coefficient theorem for a one-variable
Regge minisuperspace model.  The triangular/square law is the discrete local
form of turning-point Friedmann kinematics; its existence alone is not novel
gravitational phenomenology.

**OPEN:** the theorem is pointwise in fixed `n`.  Since `A_n=O(n^2)`, the
small parameter is effectively `n*e`; the proof does not give uniform
control as `n` approaches `1/e` or later nonlinear epochs.

**OPEN:** spatial-refinement convergence.  The fixed 600-cell leading
acceleration remains `7.90%` stronger than the preregistered continuum
Friedmann control.

**OPEN:** anisotropic stability, tensor propagation, an absolute tick,
limiting speed, Planck scales and matter masses.

## Post-result prior-art search

The search was repeated with the learned terms `weak-lapse 600-cell Regge`,
`16200 600-cell Regge cosmology`, `2n+1 Regge Friedmann recurrence` and
`n(n+1) 600-cell cosmology`.  It located the established broad
Collins--Williams/Regge recurrence literature but no primary source exposing
the exact coefficient identities above.  This negative search does not prove
novelty.

**External novelty: OPEN.**

## Next load-bearing test

The all-index question is now closed.  The next mission should not extend the
same homogeneous sequence.  It should preregister a spatially refined
`S^3` carrier, derive its cellular homogeneous action and acceleration
coefficient, and test whether the ratio to the continuum value moves from
`1.078979468...` toward `1` under a fixed radius and dust normalization.

